import React from 'react'
import Container from 'constructicon/container'

const Footer = () => (
  <Container width={40} spacing={{x: 1, t: 6, b: 3}}>
    There are definately exceptions to these rules (sometimes).
  </Container>
)

export default (Footer)
